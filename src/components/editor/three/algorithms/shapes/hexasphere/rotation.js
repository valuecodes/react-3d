import React from 'react'
import { useFrame } from 'react-three-fiber';
export default function Rotation({hexaSphere,group}) {

    // useFrame(()=>{
    //     if(group.current){
    //         console.log(group.current)
    //         // group.current.rotation.x+=0.01
    //     }
    // })

    return null
}
